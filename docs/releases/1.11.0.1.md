# MagicDesk 1.11.0.1

- Fix a crash when removing a virtual display without starting Desktop.
- Make `remove_display` safe to repeat with the same display identity, including
  after its callback wait expires.
- Report unconfirmed automation outcomes explicitly instead of suggesting unsafe
  retries. Keep terminal IDs available when launch acknowledgement is delayed.
